services:
  app:
    build: .
    ports:
      - "80:3000"
    environment:
      DATABASE_URL: "file:/app/data/dev.db"
      ADMIN_PASSWORD: "change-this-password"
      PUBLIC_BASE_URL: "http://124.220.45.252"
    volumes:
      - survey-data:/app/data
    restart: unless-stopped

volumes:
  survey-data:
